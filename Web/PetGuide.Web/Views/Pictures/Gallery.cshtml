@model PetGuide.Web.ViewModels.Pictures.Gallery

<main id="main">
    <div class="container-register">
        <div class="container">
            <div class="d-flex justify-content-center h-100">
                <div class="card" style="width: 100%">
                    <div class="card-header">
                        <h3>Gallery</h3>
                        <div class="d-flex justify-content-end social_icon">
                            <span><i class="fas fa-image"></i></span>
                            <span><i class="fas fa-dog"></i></span>
                            <span><i class="fas fa-cat"></i></span>
                        </div>
                    </div>
                    <div class="card-body">

                        <div class="row">

                            @foreach (var picture in Model.PicturesToShow)
                            {
                                <div class="col-lg-4 col-md-6 col-xs-6 thumb">
                                    <a href="@picture.Name" class="fancybox" rel="ligthbox">
                                        <img src="@picture.Name" class="zoom img-fluid " alt="">
                                    </a>
                                </div>
                            }

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

</main>

@section LoginRegisterAdd {
    <link href="~/css/login-register.css" type="text/css" rel="stylesheet" asp-append-version="true">
}